<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.ips.dao.ProxyIpsDao">
    
	<sql id="proxyIpsColumns">
				a.id AS "id",
				a.ip AS "ip",
				a.port AS "port",
				a.expire_time AS "expireTime",
				a.create_by AS "createBy.id",
				a.create_date AS "createDate",
				a.update_by AS "updateBy.id",
				a.update_date AS "updateDate",
				a.remarks AS "remarks",
				a.del_flag AS "delFlag"
	</sql>
	
	<sql id="proxyIpsJoins">
	</sql>
    
	<select id="get" resultType="ProxyIps">
		SELECT 
			<include refid="proxyIpsColumns"/>
		FROM tb_proxy_ips a
		<include refid="proxyIpsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ProxyIps">
		SELECT 
			<include refid="proxyIpsColumns"/>
		FROM tb_proxy_ips a
		<include refid="proxyIpsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="expireTime != null and expireTime != ''">
				AND a.expire_time = #{expireTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ProxyIps">
		SELECT 
			<include refid="proxyIpsColumns"/>
		FROM tb_proxy_ips a
		<include refid="proxyIpsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO tb_proxy_ips(
			id,
			ip,
			port,
			expire_time,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{ip},
			#{port},
			#{expireTime},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE tb_proxy_ips SET 	
			ip = #{ip},
			port = #{port},
			expire_time = #{expireTime},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="deleteById">
		UPDATE tb_proxy_ips SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	<select id="getips" resultType="java.util.Map">
		select ip as "ip",port as "port",expire_time as "expire_time" from tb_proxy_ips  where  expire_time > now()

		and ip
		not in
		<foreach item="item" index="index" collection="list"
				 open="(" separator="," close=")">
			#{item}
		</foreach>

	</select>
</mapper>